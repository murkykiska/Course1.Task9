<Window x:Class="Task9.Views.TextView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task9.Views"
        xmlns:vm="clr-namespace:Task9.ViewModels"
        mc:Ignorable="d"
        Title="TextView" Height="500" Width="300">
    <Window.DataContext>
        <vm:TextViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" 
                 Margin="10" 
                 Text="{Binding TextModel.Text, UpdateSourceTrigger=PropertyChanged}" />
        <Button Grid.Row="1" 
                Margin="10" 
                Content="Разбить текст на слова" 
                Command="{Binding SplitTextCommand}"/>
        <ListBox Grid.Row="2" 
                 ItemsSource="{Binding TextModel.Words, UpdateSourceTrigger=PropertyChanged}" />
    </Grid>
</Window>
